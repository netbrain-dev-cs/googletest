#include<gtest/gtest.h>

TEST(Hello, OnePlusOneShoudBeTwo)
{
	EXPECT_TRUE((1 + 1) == 2);

	EXPECT_FALSE((1 + 1) != 2);

	EXPECT_EQ(1 + 1, 2);
}



int main(int argc, char * argv[])
{
	testing::InitGoogleTest(&argc, argv);
	return RUN_ALL_TESTS();
}